# pi-web 回答格式规范

本项目 Web 界面会把带标题的回答渲染为折叠框（`##` 标题节默认折叠，最后一条消息/最后的结论节默认展开）。为了让回答在界面上呈现"过程折叠、结论一眼可见"的效果，请遵守以下格式：

## 回答结构

- 用 Markdown 标题（`##` 或 `###`）把回答**分成若干节**
- 过程性内容（调查步骤、路径对比、实现细节、代码片段）放在**前面的节**
- 最终结论放在**最后一个节**（标题可用"结论"、"总结"、"最终效果"等）
- 每个节内只写要点，简洁直接，不复述过程、不解释每个命令
- 回答开头可以有一段无标题的简短导语（结论先行），但保持一句话以内
- 禁止把大段过程写成无标题连续文本（会被整体折叠，结论反而不可见）

## 代码展示

- **不要粘贴文件完整内容**：涉及已有文件时，只写路径 + 关键点（改了哪几行、作用是什么），用要点或短代码行描述
- 必须展示代码片段时，控制在 3-5 行以内，且放入专门的 `## 代码` 节（该节默认折叠，不占视野）
- 长代码/配置文件全文一律不贴，用表格或要点总结其作用

## 示例

```markdown
已完成。

## 改动

- 新建 `.pi/settings.json`
- 修改 `server.mjs` 启动参数

## 验证

- 构建通过，压缩总结正常生成

## 结论

策略已生效，新电脑克隆即用。
```

（界面上"改动""验证"节默认折叠，"结论"节默认展开）
